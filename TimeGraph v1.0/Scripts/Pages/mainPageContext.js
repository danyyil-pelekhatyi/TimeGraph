tg.mainPage = tg.mainPage || {};

tg.mainPage.context = function (modulebuilder, sandbox) {

    function bindEvents() {
        
    };

    function registerModules() {
        
    };

    return {
        init: function(options) {
            bindEvents();
            registerModules();
        }
    };
}(tg.core.moduleBuilder, tg.core.sandbox);